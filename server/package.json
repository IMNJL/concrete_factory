{
  "name": "concrete-factory-server",
  "version": "1.0.0",
  "description": "Minimal server for serving the concrete_factory site and accepting orders",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nodemailer": "^6.9.4",
    "dotenv": "^16.3.1"
  }
}
